// Função para exibir uma mensagem de saudação
function exibirSaudacao() {
    var saudacao = "Olá, mundo!";
    alert(saudacao);
}

// Chamando a função de saudação quando a página é carregada
window.onload = function() {
    exibirSaudacao();
};